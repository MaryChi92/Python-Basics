class Road:

    mass = 25
    w = 5

    def __init__(self, length, width):
        self._length = length
        self._width = width

    def count(self):
        m = self._length * self._width * Road.mass * (Road.w / 100) / 10
        print(round(m))


r = Road(20, 5000)
r.count()